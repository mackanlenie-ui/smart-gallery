from pathlib import Path
p=Path('app/src/main/java/se/minekonomi/app/MainActivity.java')
lines=p.read_text(encoding='utf-8').splitlines()

def repl(prefix,new):
    for i,l in enumerate(lines):
        if l.startswith(prefix):
            lines[i]=new
            return
    raise SystemExit('missing '+prefix)

repl('  double unpaidBillsBetween(Calendar start,Calendar end){','''  double unpaidBillsBetween(Calendar start,Calendar end){double x=0;JSONArray a=arr("bills");Date s=start.getTime(),e=end.getTime();for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null||o.optBoolean("paid")||o.optBoolean("paused"))continue;Date d=parseDate(o.optString("due"));if(d!=null&&!d.before(s)&&!d.after(e))x+=o.optDouble("amount");}return x;}''')
repl('  double[] totals(String mk){','''  double[] totals(String mk){double in=0,out=0;JSONArray a=arr("tx");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null||!o.optString("date").startsWith(mk))continue;String t=o.optString("type");if("Inkomst".equals(t))in+=o.optDouble("amount");else if("Utgift".equals(t))out+=o.optDouble("amount");}return new double[]{in,out};} double unpaidBills(String mk){double x=0;JSONArray a=arr("bills");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o!=null&&!o.optBoolean("paid")&&!o.optBoolean("paused")&&o.optString("due").startsWith(mk))x+=o.optDouble("amount");}return x;} double categorySpend(String cat,String mk){double x=0;JSONArray a=arr("tx");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o!=null&&"Utgift".equals(o.optString("type"))&&o.optString("date").startsWith(mk)&&cat.equals(o.optString("category")))x+=o.optDouble("amount");}return x;}''')
repl('  void applyFixed(){','''  void applyFixed(){Calendar now=Calendar.getInstance();String mk=monthKey(now);int day=now.get(Calendar.DAY_OF_MONTH);JSONArray f=arr("fixed"),tx=arr("tx");Set<String> done=new HashSet<>();for(int i=0;i<tx.length();i++){JSONObject o=tx.optJSONObject(i);if(o!=null&&mk.equals(o.optString("month"))&&o.optBoolean("fromFixed"))done.add(o.optString("fixedId"));}boolean ch=false;for(int i=0;i<f.length();i++){JSONObject x=f.optJSONObject(i);if(x==null||x.optBoolean("paused"))continue;String id=x.optString("id");if(id.isEmpty()){id=UUID.randomUUID().toString();try{x.put("id",id);}catch(Exception ignored){}}int d=Math.max(1,Math.min(28,x.optInt("day",1)));if(day>=d&&!done.contains(id)){tx.put(j("name",x.optString("name"),"amount",x.optDouble("amount"),"type",x.optString("type"),"category",x.optString("category","Övrigt"),"account",x.optString("account","Lönekonto"),"date",mk+"-"+String.format(Locale.US,"%02d",d),"month",mk,"fromFixed",true,"fixedId",id));ch=true;}}if(ch){save("fixed",f);save("tx",tx);}}''')
repl('  double accountFlow(String name){','''  double accountFlow(String name){double x=0;JSONArray a=arr("tx");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null||!name.equals(o.optString("account")))continue;String t=o.optString("type");if("Inkomst".equals(t)||"TransferIn".equals(t))x+=o.optDouble("amount");else if("Utgift".equals(t)||"TransferOut".equals(t))x-=o.optDouble("amount");}return x;} void showAccounts(){shell("Konton",1);LinearLayout q=new LinearLayout(this);TextView add=small("＋ Konto",Color.rgb(50,77,101)),tr=small("⇄ Överföring",Color.rgb(50,77,101));add.setOnClickListener(v->accountDialogV20(-1));tr.setOnClickListener(v->transferDialog());q.addView(add,new LinearLayout.LayoutParams(0,-2,1));LinearLayout.LayoutParams lp=new LinearLayout.LayoutParams(0,-2,1);lp.setMargins(dp(8),0,0,0);q.addView(tr,lp);content.addView(q);section("Mina konton");JSONArray a=arr("accounts");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null)continue;final int ix=i;double bal=o.optDouble("start")+accountFlow(o.optString("name"));LinearLayout c=card();c.addView(tv(o.optString("name"),17,true));TextView typ=tv(o.optString("accountType","Lönekonto"),12,false);typ.setTextColor(MUTED);c.addView(typ);TextView s=tv("Saldo: "+fmt(bal),23,true);s.setTextColor(bal>=0?ACCENT:RED);c.addView(s);TextView ed=small("✏ Redigera konto",Color.rgb(50,77,101));ed.setOnClickListener(v->accountDialogV20(ix));c.addView(ed);content.addView(c);}} void accountDialogV20(int ix){JSONArray src=arr("accounts");JSONObject old=ix>=0?src.optJSONObject(ix):null;EditText n=new EditText(this),s=new EditText(this);n.setHint("Kontonamn");s.setHint("Startsaldo");s.setInputType(InputType.TYPE_CLASS_NUMBER|InputType.TYPE_NUMBER_FLAG_DECIMAL|InputType.TYPE_NUMBER_FLAG_SIGNED);Spinner typ=new Spinner(this);String[] types={"Lönekonto","Sparkonto","Kreditkort","Kontant","Annat"};typ.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,types));if(old!=null){n.setText(old.optString("name"));s.setText(String.valueOf(old.optDouble("start")));for(int i=0;i<types.length;i++)if(types[i].equals(old.optString("accountType","Lönekonto")))typ.setSelection(i);}LinearLayout b=new LinearLayout(this);b.setOrientation(LinearLayout.VERTICAL);b.addView(n);b.addView(s);b.addView(typ);new AlertDialog.Builder(this).setTitle(ix>=0?"Redigera konto":"Nytt konto").setView(b).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{String nn=n.getText().toString().trim();if(nn.isEmpty())return;JSONArray out=new JSONArray();String oldName=old==null?"":old.optString("name");JSONObject no=j("name",nn,"start",num(s),"accountType",typ.getSelectedItem().toString());for(int i=0;i<src.length();i++)out.put(i==ix?no:src.optJSONObject(i));if(ix<0)out.put(no);save("accounts",out);if(ix>=0&&!oldName.equals(nn))renameAccountRefsV20(oldName,nn);showAccounts();}).show();} void renameAccountRefsV20(String oldName,String newName){for(String k:new String[]{"tx","bills","planned","fixed"}){JSONArray a=arr(k);for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o!=null&&oldName.equals(o.optString("account")))try{o.put("account",newName);}catch(Exception ignored){}}save(k,a);}}''')
repl('  void showFixed(){','''  void showFixed(){shell("Fasta belopp",1);TextView add=action("＋  Nytt fast månadsbelopp");add.setOnClickListener(v->fixedDialogV20(-1));content.addView(add);section("Fasta månadsbelopp");JSONArray a=arr("fixed");if(a.length()==0)empty("Inga fasta belopp.");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null)continue;final int ix=i;boolean paused=o.optBoolean("paused");LinearLayout c=card();TextView title=tv(catIcon(o.optString("category"))+"  "+o.optString("name")+" • dag "+o.optInt("day",1),16,true);if(paused)title.setTextColor(MUTED);c.addView(title);TextView m=tv(("Inkomst".equals(o.optString("type"))?"+ ":"− ")+fmt(o.optDouble("amount"))+(paused?" • pausad":""),17,true);m.setTextColor(paused?MUTED:"Inkomst".equals(o.optString("type"))?ACCENT:RED);c.addView(m);LinearLayout row=new LinearLayout(this);TextView ed=small("✏ Redigera",Color.rgb(50,77,101)),pause=small(paused?"▶ Aktivera":"⏸ Pausa",Color.rgb(50,77,101)),del=small("🗑 Ta bort",Color.rgb(90,48,55));ed.setOnClickListener(v->fixedDialogV20(ix));pause.setOnClickListener(v->{JSONArray x=arr("fixed");JSONObject z=x.optJSONObject(ix);try{z.put("paused",!z.optBoolean("paused"));}catch(Exception ignored){}save("fixed",x);showFixed();});del.setOnClickListener(v->deleteFixedV20(ix));row.addView(ed,new LinearLayout.LayoutParams(0,-2,1));row.addView(pause,new LinearLayout.LayoutParams(0,-2,1));row.addView(del,new LinearLayout.LayoutParams(0,-2,1));c.addView(row);content.addView(c);}} void fixedDialogV20(int ix){JSONArray src=arr("fixed");JSONObject old=ix>=0?src.optJSONObject(ix):null;EditText n=new EditText(this),am=new EditText(this),day=new EditText(this);n.setHint("Namn");am.setHint("Belopp");day.setHint("Dag i månaden (1–28)");am.setInputType(2|8192);day.setInputType(2);Spinner t=new Spinner(this),cat=new Spinner(this),acc=new Spinner(this);t.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,new String[]{"Utgift","Inkomst"}));cat.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,CATS));String[] ac=accountNames();acc.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,ac));if(old!=null){n.setText(old.optString("name"));am.setText(String.valueOf(old.optDouble("amount")));day.setText(String.valueOf(old.optInt("day",1)));t.setSelection("Inkomst".equals(old.optString("type"))?1:0);for(int i=0;i<CATS.length;i++)if(CATS[i].equals(old.optString("category")))cat.setSelection(i);for(int i=0;i<ac.length;i++)if(ac[i].equals(old.optString("account")))acc.setSelection(i);}LinearLayout b=new LinearLayout(this);b.setOrientation(LinearLayout.VERTICAL);b.addView(n);b.addView(am);b.addView(day);b.addView(t);b.addView(cat);b.addView(acc);new AlertDialog.Builder(this).setTitle(ix>=0?"Redigera fast belopp":"Fast månadsbelopp").setView(b).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{if(n.getText().toString().trim().isEmpty()||num(am)<=0)return;int dy=1;try{dy=Integer.parseInt(day.getText().toString());}catch(Exception ignored){}dy=Math.max(1,Math.min(28,dy));JSONObject no=j("id",old!=null?old.optString("id",UUID.randomUUID().toString()):UUID.randomUUID().toString(),"name",n.getText().toString().trim(),"amount",num(am),"day",dy,"type",t.getSelectedItem().toString(),"category",cat.getSelectedItem().toString(),"account",acc.getSelectedItem().toString(),"paused",old!=null&&old.optBoolean("paused"));JSONArray out=new JSONArray();for(int i=0;i<src.length();i++)out.put(i==ix?no:src.optJSONObject(i));if(ix<0)out.put(no);save("fixed",out);applyFixed();showFixed();}).show();} void deleteFixedV20(int ix){JSONArray a=arr("fixed");JSONObject o=a.optJSONObject(ix);if(o==null)return;new AlertDialog.Builder(this).setTitle("Ta bort fast belopp?").setMessage(o.optString("name")).setNegativeButton("Avbryt",null).setPositiveButton("Ta bort",(d,w)->{JSONArray out=new JSONArray();for(int i=0;i<a.length();i++)if(i!=ix)out.put(a.optJSONObject(i));save("fixed",out);showFixed();}).show();}''')
repl('  void showGoals(){','''  void showGoals(){shell("Sparmål",2);TextView add=action("＋  Nytt sparmål");add.setOnClickListener(v->goalDialogV20(-1));content.addView(add);section("Mina mål");JSONArray a=arr("goals");if(a.length()==0)empty("Inga sparmål ännu.");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null)continue;final int ix=i;double target=o.optDouble("target"),saved=o.optDouble("saved"),left=Math.max(0,target-saved);LinearLayout c=card();c.addView(tv(o.optString("name"),17,true));TextView sum=tv(fmt(saved)+" av "+fmt(target)+" • "+fmt(left)+" kvar",14,false);sum.setTextColor(left<=0?ACCENT:MUTED);c.addView(sum);String due=o.optString("due");if(!due.isEmpty()){int months=monthsUntil(due);double need=months<=0?left:left/Math.max(1,months);TextView plan=tv("Måldatum "+due+" • cirka "+fmt(need)+"/månad",12,false);plan.setTextColor(MUTED);c.addView(plan);}ProgressBar pb=new ProgressBar(this,null,android.R.attr.progressBarStyleHorizontal);pb.setMax(100);pb.setProgress(target<=0?0:(int)Math.min(100,Math.round(saved/target*100)));c.addView(pb,new LinearLayout.LayoutParams(-1,dp(8)));TextView dep=small("＋ Lägg till sparande",Color.rgb(35,89,65));dep.setOnClickListener(v->goalDeposit(ix));c.addView(dep);LinearLayout row=new LinearLayout(this);TextView ed=small("✏ Redigera",Color.rgb(50,77,101)),del=small("🗑 Ta bort",Color.rgb(90,48,55));ed.setOnClickListener(v->goalDialogV20(ix));del.setOnClickListener(v->deleteGoalV20(ix));row.addView(ed,new LinearLayout.LayoutParams(0,-2,1));row.addView(del,new LinearLayout.LayoutParams(0,-2,1));c.addView(row);content.addView(c);}} void goalDialogV20(int ix){JSONArray src=arr("goals");JSONObject old=ix>=0?src.optJSONObject(ix):null;EditText n=new EditText(this),t=new EditText(this),due=new EditText(this);n.setHint("Målets namn");t.setHint("Målbelopp");t.setInputType(2|8192);Calendar cc=Calendar.getInstance();cc.add(Calendar.MONTH,6);due.setText(old!=null?old.optString("due",dateStr(cc)):dateStr(cc));due.setFocusable(false);due.setOnClickListener(v->pickDate(due));if(old!=null){n.setText(old.optString("name"));t.setText(String.valueOf(old.optDouble("target")));}LinearLayout b=new LinearLayout(this);b.setOrientation(LinearLayout.VERTICAL);b.addView(n);b.addView(t);b.addView(due);new AlertDialog.Builder(this).setTitle(ix>=0?"Redigera sparmål":"Nytt sparmål").setView(b).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{if(n.getText().toString().trim().isEmpty()||num(t)<=0)return;JSONObject no=j("name",n.getText().toString().trim(),"target",num(t),"saved",old!=null?old.optDouble("saved"):0,"due",due.getText().toString());JSONArray out=new JSONArray();for(int i=0;i<src.length();i++)out.put(i==ix?no:src.optJSONObject(i));if(ix<0)out.put(no);save("goals",out);showGoals();}).show();} void goalDeposit(int ix){EditText e=new EditText(this);e.setHint("Belopp");e.setInputType(2|8192);new AlertDialog.Builder(this).setTitle("Lägg till sparande").setView(e).setNegativeButton("Avbryt",null).setPositiveButton("Lägg till",(d,w)->{JSONArray a=arr("goals");JSONObject o=a.optJSONObject(ix);try{o.put("saved",o.optDouble("saved")+num(e));}catch(Exception ignored){}save("goals",a);showGoals();}).show();} void deleteGoalV20(int ix){JSONArray a=arr("goals");JSONObject o=a.optJSONObject(ix);if(o==null)return;new AlertDialog.Builder(this).setTitle("Ta bort sparmål?").setMessage(o.optString("name")).setNegativeButton("Avbryt",null).setPositiveButton("Ta bort",(d,w)->{JSONArray out=new JSONArray();for(int i=0;i<a.length();i++)if(i!=ix)out.put(a.optJSONObject(i));save("goals",out);showGoals();}).show();}''')
repl('  void showBills(){','''  void showBills(){shell("Räkningar",3);TextView add=action("＋  Ny räkning");add.setOnClickListener(v->billDialogV20(-1));content.addView(add);Calendar now=todayCal(),week=(Calendar)now.clone(),next=(Calendar)now.clone(),nextEnd=(Calendar)now.clone();week.add(Calendar.DAY_OF_MONTH,7);next.add(Calendar.DAY_OF_MONTH,8);nextEnd.add(Calendar.DAY_OF_MONTH,14);double w1=unpaidBillsBetween(now,week),w2=unpaidBillsBetween(next,nextEnd),salary=unpaidBillsBetween(now,salaryPeriod()[1]);LinearLayout summary=card();summary.addView(tv("Att betala snart",15,true));summary.addView(tv("Den här veckan: "+fmt(w1)+" • nästa vecka: "+fmt(w2),13,false));TextView ss=tv("Före nästa lön: "+fmt(salary),18,true);ss.setTextColor(salary>0?RED:ACCENT);summary.addView(ss);content.addView(summary);addBillTimeline();section("Räkningskalender");JSONArray a=arr("bills");ArrayList<Integer> rows=new ArrayList<>();for(int i=0;i<a.length();i++)if(a.optJSONObject(i)!=null)rows.add(i);Collections.sort(rows,(x,y)->a.optJSONObject(x).optString("due").compareTo(a.optJSONObject(y).optString("due")));if(rows.size()==0)empty("Inga räkningar ännu.");for(int pos:rows){JSONObject o=a.optJSONObject(pos);final int ix=pos;boolean paused=o.optBoolean("paused");LinearLayout c=card();int left=daysUntil(o.optString("due"));String status=paused?"Pausad":o.optBoolean("paid")?"Betald":left<0?"Försenad "+Math.abs(left)+" dagar":left==0?"Förfaller idag":left==1?"Förfaller imorgon":left+" dagar kvar";TextView title=tv(o.optString("name"),17,true);if(paused)title.setTextColor(MUTED);c.addView(title);TextView d=tv(o.optString("due")+" • "+status+(o.optBoolean("recurring")?" • återkommande":""),12,false);d.setTextColor(paused?MUTED:o.optBoolean("paid")?ACCENT:left<=3?RED:MUTED);c.addView(d);TextView am=tv(fmt(o.optDouble("amount"))+" • "+o.optString("category","Övrigt"),19,true);am.setTextColor(paused?MUTED:o.optBoolean("paid")?MUTED:RED);c.addView(am);if(!o.optBoolean("paid")&&!paused){TextView pay=small("✓ Markera betald",Color.rgb(35,89,65));pay.setOnClickListener(v->payBill(ix));c.addView(pay);}LinearLayout row=new LinearLayout(this);TextView ed=small("✏ Redigera",Color.rgb(50,77,101)),pause=small(paused?"▶ Aktivera":"⏸ Pausa",Color.rgb(50,77,101)),del=small("🗑 Ta bort",Color.rgb(90,48,55));ed.setOnClickListener(v->billDialogV20(ix));pause.setOnClickListener(v->toggleBillPauseV20(ix));del.setOnClickListener(v->deleteBillV20(ix));row.addView(ed,new LinearLayout.LayoutParams(0,-2,1));row.addView(pause,new LinearLayout.LayoutParams(0,-2,1));row.addView(del,new LinearLayout.LayoutParams(0,-2,1));c.addView(row);content.addView(c);}} void billDialogV20(int ix){JSONArray src=arr("bills");JSONObject old=ix>=0?src.optJSONObject(ix):null;EditText n=new EditText(this),am=new EditText(this),due=new EditText(this);n.setHint("Räkning");am.setHint("Belopp");am.setInputType(2|8192);due.setText(old!=null?old.optString("due",today()):today());due.setFocusable(false);due.setOnClickListener(v->pickDate(due));CheckBox rec=new CheckBox(this);rec.setText("Återkommande varje månad");rec.setTextColor(TEXT);Spinner acc=new Spinner(this),cat=new Spinner(this);String[] ac=accountNames();acc.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,ac));cat.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,BUDGET_CATS));if(old!=null){n.setText(old.optString("name"));am.setText(String.valueOf(old.optDouble("amount")));rec.setChecked(old.optBoolean("recurring"));for(int i=0;i<ac.length;i++)if(ac[i].equals(old.optString("account")))acc.setSelection(i);for(int i=0;i<BUDGET_CATS.length;i++)if(BUDGET_CATS[i].equals(old.optString("category")))cat.setSelection(i);}LinearLayout b=new LinearLayout(this);b.setOrientation(LinearLayout.VERTICAL);b.addView(n);b.addView(am);b.addView(due);b.addView(acc);b.addView(cat);b.addView(rec);new AlertDialog.Builder(this).setTitle(ix>=0?"Redigera räkning":"Ny räkning").setView(b).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{if(n.getText().toString().trim().isEmpty()||num(am)<=0)return;JSONObject no=j("name",n.getText().toString().trim(),"amount",num(am),"due",due.getText().toString(),"account",acc.getSelectedItem().toString(),"category",cat.getSelectedItem().toString(),"paid",old!=null&&old.optBoolean("paid"),"recurring",rec.isChecked(),"paused",old!=null&&old.optBoolean("paused"));JSONArray out=new JSONArray();for(int i=0;i<src.length();i++)out.put(i==ix?no:src.optJSONObject(i));if(ix<0)out.put(no);save("bills",out);showBills();}).show();} void toggleBillPauseV20(int ix){JSONArray a=arr("bills");JSONObject o=a.optJSONObject(ix);if(o==null)return;try{o.put("paused",!o.optBoolean("paused"));}catch(Exception ignored){}save("bills",a);showBills();} void deleteBillV20(int ix){JSONArray a=arr("bills");JSONObject o=a.optJSONObject(ix);if(o==null)return;new AlertDialog.Builder(this).setTitle("Ta bort räkning?").setMessage(o.optString("name")+" tas bort.").setNegativeButton("Avbryt",null).setPositiveButton("Ta bort",(d,w)->{JSONArray out=new JSONArray();for(int i=0;i<a.length();i++)if(i!=ix)out.put(a.optJSONObject(i));save("bills",out);showBills();}).show();} void payBill(int ix){JSONArray a=arr("bills");JSONObject o=a.optJSONObject(ix);if(o==null)return;JSONArray tx=arr("tx");tx.put(j("name",o.optString("name"),"amount",o.optDouble("amount"),"type","Utgift","category",o.optString("category","Boende"),"account",o.optString("account","Lönekonto"),"date",today(),"fromBill",true));save("tx",tx);if(o.optBoolean("recurring")){try{Calendar c=Calendar.getInstance();c.setTime(new SimpleDateFormat("yyyy-MM-dd").parse(o.optString("due")));c.add(Calendar.MONTH,1);o.put("due",new SimpleDateFormat("yyyy-MM-dd").format(c.getTime()));o.put("paid",false);}catch(Exception e){try{o.put("paid",true);}catch(Exception ignored){}}}else try{o.put("paid",true);}catch(Exception ignored){}save("bills",a);showBills();}''')
repl('  void showStats(){','''  void showStats(){shell("Statistik",4);int months=p.getInt("statsMonths",6);LinearLayout sel=new LinearLayout(this);for(int m:new int[]{3,6,12}){TextView b=small((months==m?"✓ ":"")+m+" mån",months==m?Color.rgb(35,89,65):Color.rgb(50,77,101));final int mm=m;b.setOnClickListener(v->{p.edit().putInt("statsMonths",mm).apply();showStats();});sel.addView(b,new LinearLayout.LayoutParams(0,-2,1));}content.addView(sel);section("Prognos");String mk=monthKey(Calendar.getInstance());double spent=totals(mk)[1];Calendar now=Calendar.getInstance();int day=now.get(Calendar.DAY_OF_MONTH),days=now.getActualMaximum(Calendar.DAY_OF_MONTH);double forecast=day<=0?spent:spent/day*days;LinearLayout fc=card();TextView ft=tv("Prognos för månadens utgifter",14,false);ft.setTextColor(MUTED);fc.addView(ft);TextView fv=tv(fmt(forecast),26,true);fv.setTextColor(AMBER);fc.addView(fv);content.addView(fc);section("Senaste "+months+" månaderna");Calendar base=Calendar.getInstance();double maxOut=1,sumOut=0,sumIn=0;for(int k=0;k<months;k++){Calendar m=(Calendar)base.clone();m.add(Calendar.MONTH,-k);double[] t=totals(monthKey(m));maxOut=Math.max(maxOut,t[1]);sumOut+=t[1];sumIn+=t[0];}LinearLayout av=card();av.addView(tv("Snitt per månad",14,true));TextView avv=tv("In "+fmt(sumIn/months)+" • Ut "+fmt(sumOut/months),16,false);avv.setTextColor(MUTED);av.addView(avv);content.addView(av);for(int k=months-1;k>=0;k--){Calendar m=(Calendar)base.clone();m.add(Calendar.MONTH,-k);String key=monthKey(m);double[] t=totals(key);LinearLayout x=card();String title=new SimpleDateFormat("MMM yyyy",SV).format(m.getTime());TextView tt=tv(title.substring(0,1).toUpperCase(SV)+title.substring(1),15,true);x.addView(tt);TextView line=tv("In "+fmt(t[0])+"  •  Ut "+fmt(t[1])+"  •  Netto "+fmt(t[0]-t[1]),13,false);line.setTextColor(t[0]-t[1]>=0?ACCENT:RED);x.addView(line);ProgressBar pb=new ProgressBar(this,null,android.R.attr.progressBarStyleHorizontal);pb.setMax(100);pb.setProgress((int)Math.round(t[1]/maxOut*100));x.addView(pb,new LinearLayout.LayoutParams(-1,dp(8)));content.addView(x);}section("Utgifter per kategori");double total=totals(mk)[1];ArrayList<JSONObject> rows=new ArrayList<>();for(String cat:BUDGET_CATS){double vv=categorySpend(cat,mk);if(vv>0)rows.add(j("cat",cat,"v",vv));}Collections.sort(rows,(a,b)->Double.compare(b.optDouble("v"),a.optDouble("v")));if(rows.size()==0)empty("Inga kategoriserade utgifter den här månaden.");for(JSONObject o:rows){String cat=o.optString("cat");double vv=o.optDouble("v");int pct=total<=0?0:(int)Math.round(vv/total*100);LinearLayout x=card();x.setClickable(true);x.setOnClickListener(v->Toast.makeText(this,cat+": "+fmt(vv)+" ("+pct+" %)",Toast.LENGTH_LONG).show());x.addView(tv(catIcon(cat)+"  "+cat,15,true));TextView am=tv(fmt(vv)+" • "+pct+" % av utgifterna",14,false);am.setTextColor(RED);x.addView(am);ProgressBar pb=new ProgressBar(this,null,android.R.attr.progressBarStyleHorizontal);pb.setMax(100);pb.setProgress(pct);x.addView(pb,new LinearLayout.LayoutParams(-1,dp(8)));content.addView(x);}section("Jämförelse");Calendar prev=Calendar.getInstance();prev.add(Calendar.MONTH,-1);double cur=totals(mk)[1],old=totals(monthKey(prev))[1],diff=cur-old;LinearLayout cmpCard=card();TextView cmp=tv((diff<=0?"↓ ":"↑ ")+fmt(Math.abs(diff))+(diff<=0?" mindre":" mer")+" än förra månaden",17,true);cmp.setTextColor(diff<=0?ACCENT:RED);cmpCard.addView(cmp);content.addView(cmpCard);}''')
repl('  void showSettings(){','''  void showSettings(){shell("Inställningar",2);section("Ekonomi");TextView pay=small("Lönedag "+salaryDay()+":e • planerat sparande "+fmt(plannedSavings()),Color.rgb(50,77,101));pay.setOnClickListener(v->paySettingsDialog());content.addView(pay);section("Säkerhet");TextView bio=small((p.getBoolean("biometricLock",false)?"✓ ":"")+"Biometriskt applås",Color.rgb(50,77,101));bio.setOnClickListener(v->{boolean nv=!p.getBoolean("biometricLock",false);p.edit().putBoolean("biometricLock",nv).apply();Toast.makeText(this,nv?"Biometriskt applås aktiverat.":"Biometriskt applås avstängt.",Toast.LENGTH_LONG).show();showSettings();});content.addView(bio);section("Notiser");TextView no=small((p.getBoolean("notifications",true)?"✓ ":"")+"Räknings- och budgetnotiser",Color.rgb(50,77,101));no.setOnClickListener(v->{p.edit().putBoolean("notifications",!p.getBoolean("notifications",true)).apply();showSettings();});content.addView(no);TextView test=small("🔔 Testa notis",Color.rgb(50,77,101));test.setOnClickListener(v->{notifyNow(2099,"Min Ekonomi","Testnotis – notiser fungerar ✅");Toast.makeText(this,"Testnotis skickad.",Toast.LENGTH_SHORT).show();});content.addView(test);section("Data");LinearLayout bk=new LinearLayout(this);TextView ex=small("Exportera backup",Color.rgb(50,77,101)),im=small("Importera backup",Color.rgb(50,77,101));ex.setOnClickListener(v->startExport());im.setOnClickListener(v->startImport());bk.addView(ex,new LinearLayout.LayoutParams(0,-2,1));LinearLayout.LayoutParams lp=new LinearLayout.LayoutParams(0,-2,1);lp.setMargins(dp(8),0,0,0);bk.addView(im,lp);content.addView(bk);TextView csv=small("Importera bank-CSV",Color.rgb(50,77,101));csv.setOnClickListener(v->startCsvImport());content.addView(csv);TextView csvout=small("Exportera transaktioner CSV",Color.rgb(50,77,101));csvout.setOnClickListener(v->exportCsvV19());content.addView(csvout);section("Om appen");TextView ver=tv("Min Ekonomi v2.0",14,true);ver.setTextColor(MUTED);content.addView(ver);}''')
repl('  void postSmartAlerts(){','''  void postSmartAlerts(){if(!p.getBoolean("notifications",true))return;JSONArray a=arr("bills");for(int i=0;i<a.length();i++){JSONObject o=a.optJSONObject(i);if(o==null||o.optBoolean("paid")||o.optBoolean("paused"))continue;int d=daysUntil(o.optString("due"));if(d>=0&&d<=3)notifyNow(1000+i,o.optString("name"),d==0?"Förfaller idag":d+" dagar kvar • "+fmt(o.optDouble("amount")));}JSONArray cb=arr("catbudgets");String mk=monthKey(Calendar.getInstance());for(int i=0;i<cb.length();i++){JSONObject o=cb.optJSONObject(i);if(o==null||o.optDouble("amount")<=0)continue;double pct=categorySpend(o.optString("category"),mk)/o.optDouble("amount")*100;if(pct>=80)notifyNow(2000+i,"Budgetvarning: "+o.optString("category"),(int)Math.round(pct)+" % av budgeten är använd.");}}''')
p.write_text('\n'.join(lines)+'\n',encoding='utf-8')
