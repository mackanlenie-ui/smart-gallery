package se.minekonomi.app;

import android.app.*;
import android.os.Bundle;
import android.content.*;
import android.graphics.Color;
import android.graphics.Typeface;
import android.graphics.drawable.GradientDrawable;
import android.text.InputType;
import android.view.*;
import android.widget.*;
import org.json.*;
import java.text.NumberFormat;
import java.text.SimpleDateFormat;
import java.util.*;

public class MainActivity extends Activity {
    private final int BG=Color.rgb(10,18,27), SURFACE=Color.rgb(20,31,42), CARD=Color.rgb(27,40,53), CARD2=Color.rgb(34,49,63);
    private final int TEXT=Color.WHITE, MUTED=Color.rgb(166,180,193), ACCENT=Color.rgb(71,200,134), RED=Color.rgb(255,108,117);
    private SharedPreferences prefs; private LinearLayout content;
    private final NumberFormat money=NumberFormat.getCurrencyInstance(new Locale("sv","SE"));
    @Override public void onCreate(Bundle b){super.onCreate(b);prefs=getSharedPreferences("min_ekonomi",MODE_PRIVATE);ensureLegacyRecurring();ensureFixedForMonth();showOverview();}
    private int dp(int x){return (int)(x*getResources().getDisplayMetrics().density+.5f);} private TextView tv(String s,int sp,boolean bold){TextView v=new TextView(this);v.setText(s);v.setTextColor(TEXT);v.setTextSize(sp);v.setTypeface(null,bold?Typeface.BOLD:Typeface.NORMAL);return v;} private GradientDrawable bg(int c,int r){GradientDrawable g=new GradientDrawable();g.setColor(c);g.setCornerRadius(dp(r));return g;} private double parse(EditText e){try{return Double.parseDouble(e.getText().toString().trim().replace(',','.'));}catch(Exception x){return 0;}} private String format(double v){return money.format(v).replace(" "," ");}
    private JSONArray arr(String k){try{return new JSONArray(prefs.getString(k,"[]"));}catch(Exception e){return new JSONArray();}} private void save(String k,JSONArray a){prefs.edit().putString(k,a.toString()).apply();} private String now(){return new SimpleDateFormat("yyyy-MM-dd",Locale.getDefault()).format(new Date());} private String monthKey(){return new SimpleDateFormat("yyyy-MM",Locale.getDefault()).format(new Date());} private boolean thisMonth(String d){return d!=null&&d.startsWith(monthKey());} private String monthTitle(){String s=new SimpleDateFormat("MMMM yyyy",new Locale("sv","SE")).format(new Date());return s.substring(0,1).toUpperCase(new Locale("sv","SE"))+s.substring(1);}
    private TextView action(String s){TextView b=tv(s,15,true);b.setGravity(Gravity.CENTER);b.setPadding(dp(14),dp(14),dp(14),dp(14));b.setBackground(bg(ACCENT,18));b.setTextColor(Color.rgb(5,28,20));b.setClickable(true);return b;} private TextView smallAction(String s,int c){TextView b=tv(s,13,true);b.setGravity(Gravity.CENTER);b.setPadding(dp(12),dp(10),dp(12),dp(10));b.setBackground(bg(c,14));b.setClickable(true);return b;}
    private void shell(String title,int selected){LinearLayout root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(BG);LinearLayout header=new LinearLayout(this);header.setOrientation(LinearLayout.VERTICAL);header.setPadding(dp(20),dp(25),dp(20),dp(9));header.addView(tv(title,29,true));if(selected==0){TextView m=tv(monthTitle(),14,false);m.setTextColor(MUTED);m.setPadding(0,dp(5),0,0);header.addView(m);}root.addView(header);ScrollView sv=new ScrollView(this);sv.setFillViewport(true);content=new LinearLayout(this);content.setOrientation(LinearLayout.VERTICAL);content.setPadding(dp(16),dp(10),dp(16),dp(25));sv.addView(content,new ScrollView.LayoutParams(-1,-2));root.addView(sv,new LinearLayout.LayoutParams(-1,0,1));root.addView(bottomNav(selected),new LinearLayout.LayoutParams(-1,dp(72)));setContentView(root);}
    private View bottomNav(int selected){LinearLayout nav=new LinearLayout(this);nav.setOrientation(LinearLayout.HORIZONTAL);nav.setPadding(dp(5),dp(5),dp(5),dp(7));nav.setBackgroundColor(SURFACE);String[] icons={"⌂","↕","◎","▤","▥"}, labels={"Översikt","Poster","Budget","Räkningar","Statistik"};View.OnClickListener[] ls={v->showOverview(),v->showTransactions(),v->showBudget(),v->showBills(),v->showStats()};for(int i=0;i<labels.length;i++){LinearLayout t=new LinearLayout(this);t.setOrientation(LinearLayout.VERTICAL);t.setGravity(Gravity.CENTER);if(i==selected)t.setBackground(bg(Color.rgb(34,61,56),16));TextView ic=tv(icons[i],22,true);ic.setGravity(Gravity.CENTER);ic.setTextColor(i==selected?ACCENT:MUTED);TextView la=tv(labels[i],10,i==selected);la.setGravity(Gravity.CENTER);la.setTextColor(i==selected?ACCENT:MUTED);t.addView(ic);t.addView(la);t.setOnClickListener(ls[i]);nav.addView(t,new LinearLayout.LayoutParams(0,-1,1));}return nav;}
    private LinearLayout card(){LinearLayout c=new LinearLayout(this);c.setOrientation(LinearLayout.VERTICAL);c.setPadding(dp(16),dp(15),dp(16),dp(15));c.setBackground(bg(CARD,22));LinearLayout.LayoutParams p=new LinearLayout.LayoutParams(-1,-2);p.setMargins(0,0,0,dp(12));c.setLayoutParams(p);return c;} private void section(String s){TextView h=tv(s,20,true);h.setPadding(dp(2),dp(14),0,dp(10));content.addView(h);} private void empty(String s){TextView e=tv(s,15,false);e.setTextColor(MUTED);e.setGravity(Gravity.CENTER);e.setPadding(dp(8),dp(25),dp(8),dp(25));content.addView(e);} private double[] totals(){double in=0,out=0;JSONArray a=arr("tx");for(int i=0;i<a.length();i++)try{JSONObject o=a.getJSONObject(i);if(thisMonth(o.optString("date"))){if("Inkomst".equals(o.optString("type")))in+=o.optDouble("amount");else out+=o.optDouble("amount");}}catch(Exception ignored){}return new double[]{in,out};}
    private void showOverview(){ensureLegacyRecurring();ensureFixedForMonth();shell("Min Ekonomi",0);double[] t=totals();double left=t[0]-t[1],budget=prefs.getFloat("budget",0);LinearLayout hero=card();hero.setPadding(dp(20),dp(20),dp(20),dp(19));hero.setBackground(bg(Color.rgb(25,54,49),26));TextView l=tv("Kvar denna månad",14,false);l.setTextColor(Color.rgb(184,216,207));hero.addView(l);TextView value=tv(format(left),34,true);value.setTextColor(left>=0?ACCENT:RED);value.setPadding(0,dp(5),0,dp(13));hero.addView(value);LinearLayout pair=new LinearLayout(this);pair.setOrientation(LinearLayout.HORIZONTAL);pair.addView(miniMetric("Inkomster",format(t[0]),ACCENT),new LinearLayout.LayoutParams(0,-2,1));pair.addView(miniMetric("Utgifter",format(t[1]),RED),new LinearLayout.LayoutParams(0,-2,1));hero.addView(pair);if(budget>0){int pct=(int)Math.round(t[1]/budget*100);TextView b=tv("Budget • "+Math.min(999,pct)+" % använt • "+format(Math.max(0,budget-t[1]))+" kvar",12,false);b.setTextColor(MUTED);b.setPadding(0,dp(14),0,dp(6));hero.addView(b);ProgressBar p=new ProgressBar(this,null,android.R.attr.progressBarStyleHorizontal);p.setMax(100);p.setProgress(Math.min(100,pct));hero.addView(p,new LinearLayout.LayoutParams(-1,dp(7)));}content.addView(hero);TextView add=action("＋  Lägg till transaktion");add.setOnClickListener(v->transactionDialog(-1));content.addView(add);section("Kommande räkningar");addUpcomingBills();section("Senaste");JSONArray a=arr("tx");if(a.length()==0)empty("Inga transaktioner ännu.");int start=Math.max(0,a.length()-4);for(int i=a.length()-1;i>=start;i--)addTxRow(a,i,false);}
    private View miniMetric(String label,String val,int color){LinearLayout c=new LinearLayout(this);c.setOrientation(LinearLayout.VERTICAL);TextView a=tv(label,12,false);a.setTextColor(MUTED);TextView b=tv(val,17,true);b.setTextColor(color);b.setPadding(0,dp(3),0,0);c.addView(a);c.addView(b);return c;} private void addUpcomingBills(){JSONArray a=arr("bills");if(a.length()==0){empty("Inga kommande räkningar.");return;}for(int i=0;i<a.length()&&i<3;i++)try{JSONObject o=a.getJSONObject(i);LinearLayout c=card(),row=new LinearLayout(this);row.setGravity(Gravity.CENTER_VERTICAL);LinearLayout txt=new LinearLayout(this);txt.setOrientation(LinearLayout.VERTICAL);TextView n=tv(o.optString("name"),16,true),d=tv("Förfaller "+o.optString("due"),12,false);d.setTextColor(MUTED);txt.addView(n);txt.addView(d);row.addView(txt,new LinearLayout.LayoutParams(0,-2,1));TextView am=tv(format(o.optDouble("amount")),16,true);am.setTextColor(RED);row.addView(am);c.addView(row);content.addView(c);}catch(Exception ignored){}}
    private void showTransactions(){shell("Poster",1);TextView add=action("＋  Ny transaktion");add.setOnClickListener(v->transactionDialog(-1));content.addView(add);TextView fixed=action("⚙  Hantera fasta månadsbelopp");fixed.setBackground(bg(Color.rgb(50,77,101),18));fixed.setTextColor(TEXT);fixed.setOnClickListener(v->showFixed());LinearLayout.LayoutParams fp=new LinearLayout.LayoutParams(-1,-2);fp.setMargins(0,dp(10),0,0);content.addView(fixed,fp);section("Alla poster");JSONArray a=arr("tx");if(a.length()==0)empty("Inga transaktioner ännu.");for(int i=a.length()-1;i>=0;i--)addTxRow(a,i,true);}
    private void addTxRow(JSONArray a,int i,boolean controls){try{JSONObject o=a.getJSONObject(i);LinearLayout c=card(),row=new LinearLayout(this);row.setGravity(Gravity.CENTER_VERTICAL);TextView icon=tv(categoryIcon(o.optString("category")),20,true);icon.setGravity(Gravity.CENTER);icon.setBackground(bg(CARD2,16));row.addView(icon,new LinearLayout.LayoutParams(dp(44),dp(44)));LinearLayout txt=new LinearLayout(this);txt.setOrientation(LinearLayout.VERTICAL);txt.setPadding(dp(12),0,dp(8),0);TextView n=tv(o.optString("name"),16,true);String extra=o.optBoolean("fromFixed")?" • fast månadsbelopp":(o.optBoolean("recurring")?" • återkommande":"");TextView sub=tv(o.optString("category")+" • "+o.optString("date")+extra,12,false);sub.setTextColor(MUTED);txt.addView(n);txt.addView(sub);row.addView(txt,new LinearLayout.LayoutParams(0,-2,1));boolean inc="Inkomst".equals(o.optString("type"));TextView m=tv((inc?"+ ":"− ")+format(o.optDouble("amount")),16,true);m.setTextColor(inc?ACCENT:RED);row.addView(m);c.addView(row);if(controls&&!o.optBoolean("fromFixed")){LinearLayout actions=new LinearLayout(this);actions.setPadding(0,dp(10),0,0);TextView edit=smallAction("Redigera",Color.rgb(48,69,88)),del=smallAction("Ta bort",Color.rgb(92,48,54));final int idx=i;edit.setOnClickListener(v->transactionDialog(idx));del.setOnClickListener(v->confirmDeleteTx(idx));actions.addView(edit,new LinearLayout.LayoutParams(0,-2,1));LinearLayout.LayoutParams lp=new LinearLayout.LayoutParams(0,-2,1);lp.setMargins(dp(8),0,0,0);actions.addView(del,lp);c.addView(actions);}content.addView(c);}catch(Exception ignored){}}
    private String categoryIcon(String c){if(c.equals("Mat"))return "🍴";if(c.equals("Boende"))return "⌂";if(c.equals("Bil/transport"))return "➜";if(c.equals("Abonnemang"))return "◉";if(c.equals("Nöjen"))return "★";if(c.equals("Hälsa"))return "+";if(c.equals("Shopping"))return "▣";if(c.equals("Lön"))return "↑";return "•";} private void confirmDeleteTx(int idx){new AlertDialog.Builder(this).setTitle("Ta bort transaktion?").setMessage("Det går inte att ångra.").setNegativeButton("Avbryt",null).setPositiveButton("Ta bort",(d,w)->{JSONArray x=arr("tx"),y=new JSONArray();for(int j=0;j<x.length();j++)if(j!=idx)try{y.put(x.getJSONObject(j));}catch(Exception ignored){}save("tx",y);showTransactions();}).show();}
    private void transactionDialog(int editIndex){JSONObject old=null;JSONArray all=arr("tx");if(editIndex>=0)try{old=all.getJSONObject(editIndex);}catch(Exception ignored){}LinearLayout box=new LinearLayout(this);box.setOrientation(LinearLayout.VERTICAL);box.setPadding(dp(20),dp(4),dp(20),0);EditText name=new EditText(this);name.setHint("Beskrivning, t.ex. ICA");EditText amount=new EditText(this);amount.setHint("Belopp");amount.setInputType(InputType.TYPE_CLASS_NUMBER|InputType.TYPE_NUMBER_FLAG_DECIMAL);Spinner type=new Spinner(this);String[] types={"Utgift","Inkomst"};type.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,types));String[] cats={"Mat","Boende","Bil/transport","Abonnemang","Nöjen","Hälsa","Shopping","Lön","Övrigt"};Spinner cat=new Spinner(this);cat.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,cats));CheckBox recurring=new CheckBox(this);recurring.setText("Återkommande varje månad");if(old!=null){name.setText(old.optString("name"));amount.setText(String.valueOf(old.optDouble("amount")));type.setSelection("Inkomst".equals(old.optString("type"))?1:0);for(int i=0;i<cats.length;i++)if(cats[i].equals(old.optString("category")))cat.setSelection(i);recurring.setChecked(old.optBoolean("recurring"));}box.addView(name);box.addView(amount);box.addView(type);box.addView(cat);box.addView(recurring);final JSONObject original=old;AlertDialog d=new AlertDialog.Builder(this).setTitle(editIndex>=0?"Redigera transaktion":"Ny transaktion").setView(box).setNegativeButton("Avbryt",null).setPositiveButton("Spara",null).create();d.setOnShowListener(x->d.getButton(-1).setOnClickListener(v->{if(name.getText().toString().trim().isEmpty()||parse(amount)<=0){Toast.makeText(this,"Fyll i namn och belopp",Toast.LENGTH_SHORT).show();return;}try{JSONObject o=new JSONObject();o.put("name",name.getText().toString().trim());o.put("amount",parse(amount));o.put("type",type.getSelectedItem().toString());o.put("category",cat.getSelectedItem().toString());o.put("date",original!=null?original.optString("date",now()):now());o.put("recurring",recurring.isChecked());if(recurring.isChecked())o.put("series",original!=null&&original.has("series")?original.optString("series"):UUID.randomUUID().toString());JSONArray src=arr("tx");if(editIndex>=0){JSONArray y=new JSONArray();for(int j=0;j<src.length();j++)y.put(j==editIndex?o:src.getJSONObject(j));save("tx",y);}else{src.put(o);save("tx",src);}d.dismiss();showTransactions();}catch(Exception ignored){}}));d.show();}
    private void showFixed(){shell("Fasta månadsbelopp",1);TextView add=action("＋  Lägg till fast belopp");add.setOnClickListener(v->fixedDialog(-1));content.addView(add);TextView info=tv("Fasta poster läggs automatiskt in en gång per månad och räknas direkt i översikten.",13,false);info.setTextColor(MUTED);info.setPadding(dp(3),dp(14),dp(3),dp(16));content.addView(info);JSONArray a=arr("fixed");if(a.length()==0)empty("Inga fasta månadsbelopp ännu.");for(int i=0;i<a.length();i++)addFixedRow(a,i);}
    private void addFixedRow(JSONArray a,int i){try{JSONObject o=a.getJSONObject(i);LinearLayout c=card(),r=new LinearLayout(this);r.setGravity(Gravity.CENTER_VERTICAL);LinearLayout txt=new LinearLayout(this);txt.setOrientation(LinearLayout.VERTICAL);TextView n=tv(o.optString("name"),17,true),s=tv(o.optString("type")+" • "+o.optString("category")+" • varje månad",12,false);s.setTextColor(MUTED);txt.addView(n);txt.addView(s);r.addView(txt,new LinearLayout.LayoutParams(0,-2,1));boolean inc="Inkomst".equals(o.optString("type"));TextView am=tv((inc?"+ ":"− ")+format(o.optDouble("amount")),17,true);am.setTextColor(inc?ACCENT:RED);r.addView(am);c.addView(r);LinearLayout actions=new LinearLayout(this);actions.setPadding(0,dp(10),0,0);TextView edit=smallAction("Redigera",Color.rgb(48,69,88)),del=smallAction("Ta bort",Color.rgb(92,48,54));final int idx=i;edit.setOnClickListener(v->fixedDialog(idx));del.setOnClickListener(v->deleteFixed(idx));actions.addView(edit,new LinearLayout.LayoutParams(0,-2,1));LinearLayout.LayoutParams lp=new LinearLayout.LayoutParams(0,-2,1);lp.setMargins(dp(8),0,0,0);actions.addView(del,lp);c.addView(actions);content.addView(c);}catch(Exception ignored){}}
    private void fixedDialog(int editIndex){JSONArray all=arr("fixed");JSONObject old=null;if(editIndex>=0)try{old=all.getJSONObject(editIndex);}catch(Exception ignored){}LinearLayout box=new LinearLayout(this);box.setOrientation(LinearLayout.VERTICAL);box.setPadding(dp(20),dp(4),dp(20),0);EditText name=new EditText(this);name.setHint("T.ex. Lön eller Hyra");EditText amount=new EditText(this);amount.setHint("Belopp");amount.setInputType(InputType.TYPE_CLASS_NUMBER|InputType.TYPE_NUMBER_FLAG_DECIMAL);Spinner type=new Spinner(this);String[] types={"Utgift","Inkomst"};type.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,types));String[] cats={"Mat","Boende","Bil/transport","Abonnemang","Nöjen","Hälsa","Shopping","Lön","Övrigt"};Spinner cat=new Spinner(this);cat.setAdapter(new ArrayAdapter<>(this,android.R.layout.simple_spinner_dropdown_item,cats));if(old!=null){name.setText(old.optString("name"));amount.setText(String.valueOf(old.optDouble("amount")));type.setSelection("Inkomst".equals(old.optString("type"))?1:0);for(int i=0;i<cats.length;i++)if(cats[i].equals(old.optString("category")))cat.setSelection(i);}box.addView(name);box.addView(amount);box.addView(type);box.addView(cat);final JSONObject original=old;AlertDialog d=new AlertDialog.Builder(this).setTitle(editIndex>=0?"Redigera fast belopp":"Nytt fast månadsbelopp").setView(box).setNegativeButton("Avbryt",null).setPositiveButton("Spara",null).create();d.setOnShowListener(x->d.getButton(-1).setOnClickListener(v->{if(name.getText().toString().trim().isEmpty()||parse(amount)<=0){Toast.makeText(this,"Fyll i namn och belopp",Toast.LENGTH_SHORT).show();return;}try{JSONObject o=new JSONObject();o.put("id",original!=null?original.optString("id"):UUID.randomUUID().toString());o.put("name",name.getText().toString().trim());o.put("amount",parse(amount));o.put("type",type.getSelectedItem().toString());o.put("category",cat.getSelectedItem().toString());JSONArray src=arr("fixed"),y=new JSONArray();if(editIndex>=0){for(int j=0;j<src.length();j++)y.put(j==editIndex?o:src.getJSONObject(j));}else{for(int j=0;j<src.length();j++)y.put(src.getJSONObject(j));y.put(o);}save("fixed",y);removeCurrentFixedTx(o.optString("id"));ensureFixedForMonth();d.dismiss();showFixed();}catch(Exception ignored){}}));d.show();}
    private void deleteFixed(int idx){new AlertDialog.Builder(this).setTitle("Ta bort fast belopp?").setMessage("Framtida månader påverkas inte längre. Den här månadens automatiska post tas också bort.").setNegativeButton("Avbryt",null).setPositiveButton("Ta bort",(d,w)->{JSONArray x=arr("fixed"),y=new JSONArray();String id="";try{id=x.getJSONObject(idx).optString("id");}catch(Exception ignored){}for(int j=0;j<x.length();j++)if(j!=idx)try{y.put(x.getJSONObject(j));}catch(Exception ignored){}save("fixed",y);removeCurrentFixedTx(id);showFixed();}).show();}
    private void removeCurrentFixedTx(String id){JSONArray x=arr("tx"),y=new JSONArray();for(int i=0;i<x.length();i++)try{JSONObject o=x.getJSONObject(i);if(o.optBoolean("fromFixed")&&id.equals(o.optString("fixedId"))&&thisMonth(o.optString("date")))continue;y.put(o);}catch(Exception ignored){}save("tx",y);} private void ensureFixedForMonth(){JSONArray fixed=arr("fixed"),tx=arr("tx");Set<String> done=new HashSet<>();for(int i=0;i<tx.length();i++)try{JSONObject o=tx.getJSONObject(i);if(o.optBoolean("fromFixed")&&thisMonth(o.optString("date")))done.add(o.optString("fixedId"));}catch(Exception ignored){}boolean changed=false;for(int i=0;i<fixed.length();i++)try{JSONObject f=fixed.getJSONObject(i);String id=f.optString("id");if(id.isEmpty()||done.contains(id))continue;JSONObject o=new JSONObject();o.put("name",f.optString("name"));o.put("amount",f.optDouble("amount"));o.put("type",f.optString("type"));o.put("category",f.optString("category"));o.put("date",monthKey()+"-01");o.put("fromFixed",true);o.put("fixedId",id);tx.put(o);changed=true;}catch(Exception ignored){}if(changed)save("tx",tx);} private void ensureLegacyRecurring(){JSONArray a=arr("tx");Set<String> done=new HashSet<>();Map<String,JSONObject> latest=new HashMap<>();for(int i=0;i<a.length();i++)try{JSONObject o=a.getJSONObject(i);if(!o.optBoolean("recurring"))continue;String s=o.optString("series");if(s.isEmpty()){s=UUID.randomUUID().toString();o.put("series",s);}if(thisMonth(o.optString("date")))done.add(s);JSONObject p=latest.get(s);if(p==null||o.optString("date").compareTo(p.optString("date"))>0)latest.put(s,o);}catch(Exception ignored){}boolean changed=false;for(Map.Entry<String,JSONObject> e:latest.entrySet())if(!done.contains(e.getKey()))try{JSONObject n=new JSONObject(e.getValue().toString());n.put("date",monthKey()+"-01");a.put(n);changed=true;}catch(Exception ignored){}if(changed)save("tx",a);}
    private void showBudget(){shell("Budget",2);double b=prefs.getFloat("budget",0),spent=totals()[1];LinearLayout c=card();TextView label=tv("Månadsbudget",14,false);label.setTextColor(MUTED);TextView v=tv(format(b),29,true);c.addView(label);c.addView(v);if(b>0){int pct=(int)Math.min(100,Math.round(spent/b*100));TextView x=tv(format(spent)+" använt • "+format(Math.max(0,b-spent))+" kvar",13,false);x.setTextColor(MUTED);x.setPadding(0,dp(10),0,dp(6));c.addView(x);ProgressBar p=new ProgressBar(this,null,android.R.attr.progressBarStyleHorizontal);p.setMax(100);p.setProgress(pct);c.addView(p,new LinearLayout.LayoutParams(-1,dp(8)));}content.addView(c);TextView edit=action("Ändra månadsbudget");edit.setOnClickListener(vw->budgetDialog());content.addView(edit);categorySpend();} private void budgetDialog(){EditText e=new EditText(this);e.setHint("t.ex. 15000");e.setInputType(InputType.TYPE_CLASS_NUMBER|InputType.TYPE_NUMBER_FLAG_DECIMAL);float old=prefs.getFloat("budget",0);if(old>0)e.setText(String.valueOf(old));new AlertDialog.Builder(this).setTitle("Månadsbudget").setView(e).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{prefs.edit().putFloat("budget",(float)parse(e)).apply();showBudget();}).show();}
    private void categorySpend(){Map<String,Double> m=new LinkedHashMap<>();JSONArray a=arr("tx");for(int i=0;i<a.length();i++)try{JSONObject o=a.getJSONObject(i);if(thisMonth(o.optString("date"))&&"Utgift".equals(o.optString("type"))){String k=o.optString("category");m.put(k,m.containsKey(k)?m.get(k)+o.optDouble("amount"):o.optDouble("amount"));}}catch(Exception ignored){}section("Utgifter per kategori");if(m.isEmpty())empty("Ingen förbrukning att visa.");for(Map.Entry<String,Double> e:m.entrySet()){LinearLayout c=card(),l=new LinearLayout(this);TextView n=tv(e.getKey(),16,true),v=tv(format(e.getValue()),16,true);l.addView(n,new LinearLayout.LayoutParams(0,-2,1));l.addView(v);c.addView(l);content.addView(c);}}
    private void showBills(){shell("Räkningar",3);TextView add=action("＋  Lägg till räkning");add.setOnClickListener(v->billDialog());content.addView(add);section("Kommande");JSONArray a=arr("bills");if(a.length()==0)empty("Inga räkningar inlagda.");for(int i=0;i<a.length();i++)try{JSONObject o=a.getJSONObject(i);LinearLayout c=card();TextView n=tv(o.optString("name"),17,true),sub=tv("Förfaller "+o.optString("due"),13,false),am=tv(format(o.optDouble("amount")),20,true);sub.setTextColor(MUTED);am.setTextColor(RED);c.addView(n);c.addView(sub);c.addView(am);final int idx=i;TextView paid=smallAction("Markera betald / ta bort",Color.rgb(48,69,88));paid.setOnClickListener(v->{JSONArray x=arr("bills"),y=new JSONArray();for(int j=0;j<x.length();j++)if(j!=idx)try{y.put(x.getJSONObject(j));}catch(Exception ignored){}save("bills",y);showBills();});c.addView(paid);content.addView(c);}catch(Exception ignored){}} private void billDialog(){LinearLayout box=new LinearLayout(this);box.setOrientation(LinearLayout.VERTICAL);EditText n=new EditText(this);n.setHint("Räkning");EditText a=new EditText(this);a.setHint("Belopp");a.setInputType(InputType.TYPE_CLASS_NUMBER|InputType.TYPE_NUMBER_FLAG_DECIMAL);EditText due=new EditText(this);due.setHint("Förfallodatum, t.ex. 2026-09-28");box.addView(n);box.addView(a);box.addView(due);new AlertDialog.Builder(this).setTitle("Ny räkning").setView(box).setNegativeButton("Avbryt",null).setPositiveButton("Spara",(d,w)->{if(n.getText().toString().trim().isEmpty()||parse(a)<=0)return;try{JSONObject o=new JSONObject();o.put("name",n.getText().toString().trim());o.put("amount",parse(a));o.put("due",due.getText().toString().trim());JSONArray x=arr("bills");x.put(o);save("bills",x);showBills();}catch(Exception ignored){}}).show();}
    private void showStats(){shell("Statistik",4);JSONArray a=arr("tx");Map<String,double[]> months=new TreeMap<>(Collections.reverseOrder());for(int i=0;i<a.length();i++)try{JSONObject o=a.getJSONObject(i);String d=o.optString("date");if(d.length()<7)continue;String m=d.substring(0,7);double[] v=months.containsKey(m)?months.get(m):new double[]{0,0};if("Inkomst".equals(o.optString("type")))v[0]+=o.optDouble("amount");else v[1]+=o.optDouble("amount");months.put(m,v);}catch(Exception ignored){}if(months.isEmpty())empty("Statistik visas när du lagt in transaktioner.");for(Map.Entry<String,double[]> e:months.entrySet()){LinearLayout c=card();c.addView(tv(e.getKey(),18,true));TextView i=tv("In: "+format(e.getValue()[0]),15,false),o=tv("Ut: "+format(e.getValue()[1]),15,false),r=tv("Resultat: "+format(e.getValue()[0]-e.getValue()[1]),17,true);i.setTextColor(ACCENT);o.setTextColor(RED);c.addView(i);c.addView(o);c.addView(r);content.addView(c);}categorySpend();}
}
